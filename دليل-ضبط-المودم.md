# دليل ضبط مودم LINKSYS — نظام منيو مسخن المحلي

هذا الدليل يشرح ضبط مودم LINKSYS والمشروع ليعمل **نظام المنيو على الشبكة المحلية**
مع QR + NFC لكل طاولة. ينطبق على واجهتَي Linksys الشائعتين: **Smart Wi-Fi** (EA/velop
والإصدارات الحديثة) و**الكلاسيكية/ISP** (شبيهة WRT القديمة) — مع الإشارة للفرق بينهما.

---

## 1) ضبط IP ثابت لجهاز السيرفر (مهم جداً)

عنوان QR لكل طاولة يُبنى من عنوان جهاز السيرفر (مثل `http://192.168.1.10/menu?t=...`).
يجب أن يكون ثابتاً حتى لا تنقطع الروابط.

> **العنوان الذي يفتحه الزبائن = عنوان السيرفر على نفس شبكة الواي-فاي** التي يتصل بها
> العملاء. إذا كان في جهاز السيرفر أكثر من بطاقة شبكة (واي-فاي + سلكية) فتأكد أن
> `LAN_IP` هو العنوان المرئي من جوالات الواي-فاي، وإلا لا تعمل روابط QR من الطاولات.
> اسأل المودم: افتح من جوال متصل بالواي-فاي `http://192.168.1.10/portal.html` — إن
> فُتحت فبالضبط هذا هو العنوان الصحيح.

### الطريقة الأسهل — من إعدادات المودم (Static DHCP / Address Reservation):

**أ) واجهة Smart Wi-Fi (EA / velop / أغلب الموديلات الحديثة):**
1. ادخل للوحة المودم: `http://192.168.1.1` أو `http://myrouter.local`
   (اسم المستخدم: `admin` وكلمة المرور هي التي ضبطتها عند الإعداد الأول، أو انظر ملصق Router على ظهر الجهاز)
2. من القائمة: **Connectivity → Basic Settings**
3. انزل إلى قسم الأجهزة المتصلة (Connected Devices) واختر جهاز السيرفر، أو ابحث
   عن خيار **Static DHCP** / **Address Reservation**
4. فعّل "Static" (ثابت) لجهاز السيرفر وخصص له عنواناً **خارج نطاق DHCP** حتى لا
   يتزاحم مع الجوالات:
   - اجعل نطاق DHCP مثلاً `192.168.1.50 – 192.168.1.250`
   - ثم ضع جهاز السيرفر على `192.168.1.10`
5. احفظ (Apply). يمكن نفس الخطوة من تطبيق **Linksys** الجوال: الأجهزة → جهاز السيرفر → IP ثابت.

**ب) الواجهة الكلاسيكية (ISP/شبيهة WRT):**
1. `http://192.168.1.1` (غالباً `admin` وكلمة المرور على الملصق، أو فارغة/`admin`)
2. **Setup → Basic Setup** → تأكد أن IP المودم `192.168.1.1`
3. **Setup → Basic Setup → Network Address Server Settings (DHCP)**:
   - املأ **Start IP Address** بـ `192.168.1.50` لتحرير الأعلى
   - في جدول **Address Reservation**: سطر جديد → MAC جهاز السيرفر ← `192.168.1.10` → Enable
4. **Save Settings → Restart**

### بديل — من جهاز السيرفر نفسه (Windows):
شغّل `set-static-ip.bat` كمسؤول وأدخل:
- IP الثابت: `192.168.1.10`
- الجيت واي: `192.168.1.1`

(حتى مع IP يدوي على الجهاز، يبقى الحجز في المودم أفضل لأنه يمنع أي جهاز من أخذ نفس العنوان.)

### ثم حدّث الإعداد:
```
LAN_IP=192.168.1.10
```
- في التطوير: ملف `.env` في جذر المشروع.
- بعد التثبيت كبرنامج: `C:\ProgramData\MenuOne\mesakhen\config.env` (مع إعادة تشغيل البرنامج).

أعد تشغيل السيرفر، وأعد توليد/طباعة رموز QR من لوحة التحكم
(الرابط `طباعة QR/NFC الطاولات`) — لأن QR القديم يحمل عنوان IP قديماً.

---

## 2) تشغيل السيرفر

- **كبرنامج مثبّت:** ثبّت `MenuOne-mesakhen-Setup.exe` ثم شغّل اختصار المكتب — الخدمة
  تعمل بالأساس في الخلفية وتفتح منفذين: **80** (افتراضي روابط الزبائن) و**3000**.
- **من الكود المصدري (تطوير):** من مجلد المشروع شغّل `start.bat` أو `npm start`.

تحقق من الوصول من جوال على نفس واي-فاي المطعم:
- صفحة الزبون: `http://192.168.1.10/portal.html` (وبلا :3000 أيضاً تعمل)
- منيو طاولة: `http://192.168.1.10/menu?t=<الرمز>`
- لوحة التحكم: `http://192.168.1.10:3000/admin/login.html`

**ملاحظة جدار الحماية (Windows):** عند أول تشغيل سيظهر تنبيه — اسمح لـ Node.js/MenuOne
بالوصول عبر **الشبكات الخاصة** (Private networks). المثبّت يضيف استثناء الجدار تلقائياً.

---

## 3) طباعة أوراق الطاولات (QR + NFC)

1. ادخل للوحة التحكم: `http://192.168.1.10:3000/admin/login.html`
2. من الشريط الجانبي اضغط **طباعة QR/NFC الطاولات**
3. الصفحة تعرض بطاقة لكل طاولة:
   - **جهة اليمين:** رمز QR للطاولة (يفتح المنيو مباشرة)
   - **جهة اليسار:** تاغ NFC (نفس الرابط يُكتب في تاغ NFC) + **كود الطاولة 6 أرقام**
     + اسم وكلمة مرور الواي فاي
4. اضغط **طباعة** — ستطبع كل الطاولات (1 → 15 افتراضيًا)

> **لا تُعد طباعة عند تغيير السيرفر إلا لو تغيّر العنوان**: الـ QR يحمل `http://LAN_IP/menu?t=...`
> — إذا بقي `LAN_IP` ثابتاً (الحجز أعلاه) فالطباعة القديمة صالحة للأبد.
> أما **كود الـ 6 أرقام** فلا يتغير إلا بزر «كود جديد» من تعديل الطاولة (بعد ذلك تُعاد
> طباعة تلك البطاقة وحدها). البطاقات القديمة المطبوعة بـ 5 أرقام تعمل حاليًا لفترة توافق؛
> بعد إعادة طباعة الكل اجعل النظام صارمًا بإضافة `CARD_CODE_ALLOW_LEGACY_5=0` في config.env.
> ملاحظة أمان: تجربة كود خاطئ تتصاعد إلى **إغلاق الكود مؤقتًا** — لا يُعقَد هذا العميل
> الشرعي (8 محاولات على نفس الكود تقفل الكود وحده، وأقصى 10 محاولات/دقيقة لكل جهاز).

---

## 4) إعداد تاغات NFC

تاغات NFC القابلة للبرمجة (NTAG213 أو NTAG215) تُكتب فيها نفس رقم الرابط لكل طاولة:

```
http://192.168.1.10/menu?t=<رمز الطاولة>
```

طريقة الكتابة بأسهل وسيلة:
- **نظام iOS:** تطبيق "NFC Tools" → Write → امسح التاغ → أكتب الرابط
- **نظام Android:** تطبيق "NFC Tools" أيضاً (أو إعدادات NFC لبعض الهواتف تدعم كتابة الروابط)

**مهم:** على iOS يجب كتابة الرابط كـ **"URL record"** وليس نصاً عادياً
حتى يفتح Safari مباشرة. تطبيق NFC Tools يقوم بذلك تلقائياً عند اختيار
Write → Add a record → URL.

الرقم المطلوب في لوحة كل طاولة: الصق التاغ داخل لوحة خشبية أو بلاستيكية فوق الطاولة،
وبجانبه ضع ورقة QR المطبوعة. العميل الذي يعرف NFC يقرب هاتفه، والآخر يمسح QR — وكلاهما يصل لنفس المنيو.

---

## 5) الواي فاي للعملاء + شاشة بوابة الاتصال (Captive Portal)

NFC/QR لا يستطيعان إجبار الهاتف على الاتصال بالواي فاي ثم فتح الرابط **في خطوة واحدة** بدون أدوات إضافية.
لذلك ننصح بـ:

### أ) طباعة اسم وكلمة مرور الواي فاي على كل بطاقة طاولة (موجود في صفحة الطباعة أعلاه):
ضع اسم الشبكة وكلمة السر واضحة على لوحة الطاولة أو على المنضدة.

### ب) شبكة الضيوف في LINKSYS — احذر العزل:
إذا استخدمت **Guest WiFi** (Connectivity → Guest Access) للعملاء فتأكد من **إيقاف
"Guest Network Isolation"** أو لا تستخدمها للمشروع: شبكة الضيوف المعزولة تمنع الجوالات
من الوصول لجهاز السيرفر المحلي فتتقطع الروابط. الأبسط والأأمن: شبكة واحدة رئيسية للكل.

### ج) تحويل شاشة "تسجيل الدخول للشبكة" (Captive Portal):
عند اتصال الهاتف بشبكة بدون إنترنت يفتح نظام الهاتف شاشة بوابة (iOS عبر
`captive.apple.com`، أندرويد عبر `connectivitycheck.gstatic.com`). بعض موديلات
LINKSYS (خصوصاً مع برمجيات DD-WRT/ExpressForward) تحتوي "Guest Portal/Hotspot":
1. ابحث عن **Guest Access / Portal / Hotspot / Sign-in Page** في إعدادات المودم
2. إن وُجد خيار "Redirect HTTP to URL" → ضع: `http://192.168.1.10/portal.html`
3. إن لم يوجد، ولا تريد إنترنت للعملاء: تظل شاشة أندرويد المدمجة تظهر أحيانًا،
   والنظام يعالجها جيدًا (انظر فقرة "شاشة البوابة المدمجة" أدناه).

**اختياري متقدم — ربط المودم بالجلسات (نظام الكروت):** يستطيع المودم/نظام الكرت
استعلام السيرفر عن حالة جلسة الطاولة عبر:
```
GET http://192.168.1.10/api/session/gate/<TOKEN>?mac=AA:BB:..&key=GATE_KEY
```
(المفتاح `GATE_KEY` يُضبط في config.env) فيمنح الإنترنت للطاولة النشطة فقط ويطردها عند الإنهاء.

### د) شاشة البوابة المدمجة في أندرويد (معروفة — كيف يتصرف الطاقم):
أحياناً يفتح أندرويد شاشة "الشبكة تتطلب تسجيل دخول" (WebView نظامي) بدل المتصفح.
هذه الشاشة **لا تسمح بفتح الكاميرا من الموقع** (قيد نظام). في الصفحة:
- زر «امسح رمز الطاولة» إن فشل فتح الكاميرا خلال لحظة يتحوّل إلى «افتح المسح في
  المتصفح» — اضغطه لينتقل العميل للمتصفح حيث يعمل المسح.
- وإن بقيت الشاشة لا تستجيب: **أبسط حل يعمل 100%** — أغلق النافذة وافتح **كاميرا
  الهاتف الأصلية** على رمز QR بالبطاقة (تفتح المنيو مباشرة)، أو أدخل كود 6 أرقام
  في الخانة. كلتا الطريقتين لا تحتاج أي إعداد.

### هـ) بديل عملي بدون Captive Portal (الموصى به فعلياً):
- الطلب من العميل: اتصال بالواي فاي → ثم **مسح QR البطاقة بكاميرا هاتفه مباشرة**
  (أو إدخال الكود على `http://192.168.1.10/portal.html`). هذه هي التجربة الواقعية
  في أغلب المطاعم وتعمل مع 100% من الأجهزة بلا أي إعداد خاص في المودم.

---

## 6) أمان الشبكة القوي (LINKSYS) — قائمة إلزامية

| # | البند | أين / القيمة |
|---|-------|------|
| 1 | تشفير الواي-فاي | WPA3-SAE إن كان مدعومًا، وإلا **WPA2-AES فقط** (لا TKIP ولا WEP). في Smart Wi-Fi: Wireless Settings → Security. |
| 2 | كلمة مرور شبكة قوية | 14+ حرفًا عشوائية. لا تُطبع على بطاقات الطاولات — بدلا منها اكتب «اسأل الكاشير عن كلمة الواي فاي» أو استخدم QR الواي فاي المطبوع بالفعل. |
| 3 | تعطيل WPS | إعدادات لاسلكية متقدمة → WPS = Disable (ثغرة اختراق معروفة). |
| 4 | تعطيل UPnP | Connectivity → Network Administration → UPnP = Disable (أجهزة مجهولة تفتح منافذ بنفسها). |
| 5 | تعطيل الإدارة عن بُعد | Remote Management / Access from the Internet = Disable — اللوحة تعمل من شبكة LAN فقط. |
| 6 | كلمة مرور المودم | غيّر كلمة admin الافتراضية، واربطها بحرف/رقم لا تُدرجه بالصور. لا تترك `admin/admin`. |
| 7 | تحديث Firmware | Connectivity → Connectivity Administration → Firmware Update — ثغرات المودم هي ثغرات نظامك. |
| 8 | جيت وي-فاي واحد ثابت | DHCP reservation للسيرفر (`192.168.1.10`) — انظر القسم 1. |
| 9 | شبكة الضيوف | **إما** لا تستخدم Guest (شبكة واحدة رئيسية للكل، وهي الأبسط والأأمن للمشروع المحلي) — **وإما** استخدمها مع إلغاء «Guest Isolation» حتمًا وإلا لن تفتح الطاولات المنيو. |
| 10 | بلا فتح منافذ | لا Port Forwarding ولا DMZ ولا DDNS للمنيو: كل شيء داخل الشبكة فقط. لا توجد حالة مشروعة تحتاج منفذًا مفتوحًا للإنترنت. |
| 11 | سيرفر موصول سلكيًا | كابل Ethernet من جهاز السيرفر إلى المودم أسرع وأثبت من واي-فاي للزبائن الكثيفين (لكن تأكد أن `LAN_IP` يشير للعنوان الذي يراه الزبائن — انظر ملاحظة القسم 1). |
| 12 | جدار ويندوز للسيرفر | `open-firewall.bat` يفتح استثناءً لـ Node/MenuOne للشبكات الخاصة فقط. لا تفتح اللوحة على «Public». |

### إذا أردت عزلًا حقيقيًا (لديك مودم يدعم VLAN / Multi-SSID مخصصًا)
- شبكة **إدارة** (الكاشير + السيرفر) وشبكة **ضيوف** منفصلتان.
- قاعدة جدار/إذن وحيدة من الضيوف: **TCP 80 و3000 نحو IP السيرفر فقط**، وكل ما عداها ممنوع.
- لوحة تحكم المنيو تبقى في شبكة الإدارة. (لا توفره أجهزة Home Linksys القياسية — عندكئها استخدم شبكة واحدة.)

---

## 7) إعدادات إضافية في المودم (اختياري)

### تحويل المنفذ (Port Forwarding) — غير ضروري على الشبكة المحلية:
كل الأجهزة داخل المودم نفسه فلا حاجة له. استخدمه فقط إذا أردت الوصول **من خارج
الشبكة** (مثلاً من إنترنت المودم نفسه عبر عنوان عام):
- **Smart Wi-Fi:** Connectivity → Games and Applications (أو قسم Port Forwarding)
- **الكلاسيكية:** Applications & Gaming → Port Forwarding
- External `80` → Internal `192.168.1.10:80` (TCP) — ولوحة التحكم `3000`.

### تشغيل السيرفر تلقائياً عند إقلاع الجهاز:
- **Windows (البرنامج المثبّت):** handled تلقائياً — المثبّت يسجل تشغيلًا صامتًا
  عند الإقلاع (منيو ون في الخلفية على المنافذ 80/3000).
- **Windows (تطوير من الكود المصدري):** ضع اختصار `start.bat` في `shell:startup` أو Task Scheduler.
- **Linux:** انسخ `mesakhen.service` إلى `/etc/systemd/system/` بعد ضبط المسارات، ثم:
  ```bash
  sudo systemctl enable mesakhen
  sudo systemctl start mesakhen
  ```

### صور المنيو (خفيف وسريع):
بعد إضافة صور أطباق جديدة شغّل `node tools/optimize-images.js` ثم أعد البناء — تُخدَم
نسخ WebP/JPEG أخف تلقائياً مع كاش أسبوع لكل الأجهزة.

---

## 8) تشخيص سريع: «المنيو لا يفتح» من الجوال

| العَرَض على شاشة الجوال | السبب المرجّح | الحل |
|---|---|---|
| خطأ اتصال عند رابط يبدأ بـ `192.168.1.100` أو `192.168.1.2` | **بطاقة قديمة** مطبوعة بعنوان السيرفر القديم | أعد الطباعة من اللوحة (تطبع الآن العنوان الصحيح) واستبدل البطاقات على الطاولات |
| الصفحة تدور بلا نتيجة مع أن الجوال على واي-فاي المطعم | الجوال على شبكة/باند لا يصل السيرفر (عزل لاسلكي بين APs) أو على بيانات الهاتف | وصّل الجوال بشبكة المطعم المطلوبة؛ جرّب فتح `http://192.168.1.150` مباشرة — إن فُتحت فالمشكلة عنوان البطاقة لا السيرفر |
| `menu.lan` لا يفتح | الـ DNS داخل الراوتر لا يحوّل الاسم للسيرفر | اضبط DNS/معيار الاسم في الراوتر كما في القسم 5، أو احذف السطر من الطباعة الجديدة مؤقتًا |
| شاشة «الشبكة تتطلب تسجيل دخول» وتوقف | بوابة أندرويد المدمجة | أدخل الكود فيها، أو أغلقها وافتح كاميرا الهاتف على QR — وكلاهما يعمل (القسم 5د) |
| زر الكاميرا لا يستجيب داخل شاشة البوابة | قيد WebView المدمج المعروف | «افتح المسح في المتصفح» أو مسح مباشر من كاميرا الهاتف |
| «الكود غير صحيح» مع كود نظره صحيح | الأكواد تغيّرت (إعادة توليد) أو الخطأ بين O/0 | الأكواد الحالية 6 أرقام من بطاقة مطبوعة حديثة؛ حدّث البطاقات |
| اللوحة تقول «عنوان IP غير مصرح للوحة التحكم» (403) | أمان اللوحة مقصور على `ADMIN_ALLOW_IPS` (السيرفر فقط حاليًا) | ادخل من جهاز السيرفر نفسه، أو أضف عنوان جهاز الإدارة إلى `ADMIN_ALLOW_IPS` في config.env وأعد تشغيل الخدمة |

**قاعدة إصبع سريع:** افتح من جوال على نفس الشبكة `http://192.168.1.150/portal.html` — إن فُتحت البوابة فالسيرفر والشبكة بخير، وأي فشل عند الطاولات هو من بطاقات قديمة أو جوال على شبكة أخرى.

---

## 9) قائمة فحص سريعة

| # | البند | الحالة |
|---|-------|--------|
| 1 | IP ثابت لجهاز السيرفر (192.168.1.10) عبر Static DHCP في مودم LINKSYS أو set-static-ip.bat | ☐ |
| 2 | تحديث `LAN_IP=192.168.1.10` في .env / config.env وإعادة تشغيل السيرفر | ☐ |
| 3 | السماح بالوصول عبر جدار حماية Windows (يُضاف تلقائياً مع المثبّت) | ☐ |
| 4 | فتح `http://192.168.1.10/portal.html` من جوال متصل بالواي-فاي نفسه | ☐ |
| 5 | فتح لوحة التحكم وطباعة QR/NFC للطاولات (مع كود 6 أرقام + بيانات الواي-فاي) | ☐ |
| 6 | كتابة تاغات NFC بروابط الطاولات (تطبيق NFC Tools) | ☐ |
| 7 | اختبار: اتصال هاتف بالواي فاي → مسح QR بالكاميرا → فتح المنيو | ☐ |
| 8 | اختبار: زر «امسح رمز الطاولة» في المتصفح يفتح الكاميرا ويفكّ الرمز | ☐ |
| 9 | اختبار: لمس NFC بالهاتف → فتح المنيو | ☐ |
| 10 | عدم تفعيل "Guest Isolation" على شبكة العملاء | ☐ |
| 11 | إنهاء جلسة من اللوحة → جوال الطاولة لا يبدأ جلسة إلا بمسح/كود جديد | ☐ |
